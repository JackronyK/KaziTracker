services:
  postgres:
      image: postgres:15-alpine
      container_name: kazitracker-postgres
      env_file: backend/backend.env
      ports:
        - "5432:5432"
      volumes:
        - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
